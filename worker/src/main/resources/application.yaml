spring:
  application:
    name: worker

server:
  port: ${port:8081}

eureka:
  instance:
      hostname: ${hostname:worker}
  client:
    registerWithEureka: true
    service-url:
      defaultZone: ${eureka:http://eureka:8761}/eureka

logging:
  level:
    com.tvelykyy: DEBUG
