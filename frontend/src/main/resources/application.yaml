spring:
  application:
    name: frontend

server:
  port: ${port:8080}

eureka:
  instance:
      hostname: ${hostname:localhost}
  client:
    registerWithEureka: true
    service-url:
      defaultZone: ${eureka:http://localhost:8761}/eureka

logging:
  level:
    com.tvelykyy: DEBUG
